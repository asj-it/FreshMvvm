name: Publish Packages

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      run: dotnet restore src/FreshMvvm.sln 
    - name: Build
      run: dotnet build --configuration Release --no-restore src/FreshMvvm/FreshMvvm.csproj 
    - name: Publish FreshMvvm.ashjit
      uses: brandedoutcast/publish-nuget@v2.5.2
      with:
          PROJECT_FILE_PATH: src/FreshMvvm/FreshMvvm.csproj
          NUGET_KEY: ${{secrets.NUGET_API_KEY}}
          NUGET_SOURCE: https://api.nuget.org
          PACKAGE_NAME: FreshMvvm.asj
